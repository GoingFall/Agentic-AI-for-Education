# Agent 运行时（discussion §3.2：基于 LangChain）
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# 向量库（Chroma）
chromadb>=0.5.0

# PDF 预处理（§3.3.1：PyMuPDF 默认；可选 PDF-Extract-Kit 需单独克隆并设置 PDF_EXTRACT_KIT_ROOT）
pymupdf>=1.24.0
# 使用 PDF-Extract-Kit 后端时需生成临时 config，依赖 PyYAML
PyYAML>=6.0
# pdfplumber>=0.11.0
# pypdf>=4.0.0

# 环境与 API（.env + LLM）
python-dotenv>=1.0.0

# 嵌入与 LLM 接入（可选其一或多种）
langchain-openai>=0.2.0
# langchain-anthropic>=0.2.0
# litellm>=1.0.0

# 知识图谱（首版直接使用 Neo4j）
neo4j>=5.0.0

# 演示前端（Dash，tasks 6.x，含 background callbacks）
dash[diskcache]>=2.14.0
diskcache>=5.6.0
dash-bootstrap-components>=1.6.0
markdown>=3.5.0

# REST API（tasks 7.x）
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0

# 展示用（可选：scripts/show_task2_results.py 表格与 HTML 导出）
# rich>=13.0.0
