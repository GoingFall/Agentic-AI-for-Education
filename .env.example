# 复制为 .env 并填入真实值。.env 已被 .gitignore 忽略，勿提交。

# OpenRouter（必填，用于 LLM 调用）
OPENROUTER_API_KEY=
# 可选，默认 openai/gpt-oss-120b
OPENROUTER_MODEL=openai/gpt-3.5-turbo

# Neo4j（知识图谱）。不填 NEO4J_PASSWORD 时，运行 tests/test_neo4j_connection.py 会跳过
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=

# Chroma 向量库持久化目录（可选，默认项目下 chroma_db）
# CHROMA_PERSIST_DIR=./chroma_db
