[pytest]
# 抑制 Pydantic v1 兼容层在 Python 3.13+ 触发的 ForwardRef._evaluate 弃用警告（来自依赖）
filterwarnings =
    ignore::DeprecationWarning:pydantic.v1.typing
markers =
    slow: 耗时较长，默认可排除（如 -m "not slow"）
    performance: 性能/压力测试
    e2e: 端到端测试，依赖外部服务
